

# 🚀 Reverse SSH Tunnel Manager

یک اسکریپت جامع و خودکار برای ایجاد تونل معکوس پایدار بین سرور خارج (Foreign) و سرور ایران (Internal). این روش به دلیل ماهیت **Reverse Connection**، حتی در زمان اختلالات شدید اینترنت که دسترسی از خارج به ایران محدود می‌شود، به خوبی عمل می‌کند.

---

## 📋 فهرست مطالب

* [ویژگی‌ها](https://www.google.com/search?q=%23-%D9%88%DB%8C%DA%98%DA%AF%DB%8C%E2%80%8C%D9%87%D8%A7)
* [پیش‌نیازها](https://www.google.com/search?q=%23-%D9%BE%DB%8C%D8%B4%E2%80%8C%D9%86%DB%8C%D8%A7%D8%B2%D9%87%D8%A7)
* [نصب و اجرا](https://www.google.com/search?q=%23-%D9%86%D8%B5%D8%A8-%D9%88-%D8%A7%D8%AC%D8%B1%D8%A7)
* [ساختار فنی](https://www.google.com/search?q=%23-%D8%B3%D8%A7%D8%AE%D8%AA%D8%A7%D8%B1-%D9%81%D9%86%DB%8C)
* [عیب‌یابی](https://www.google.com/search?q=%23-%D8%B9%DB%8C%D8%A8%E2%80%8C%DB%8C%D8%A7%D8%A8%DB%8C)

---

## ✨ ویژگی‌ها

* 
**پایداری بالا:** استفاده از `autossh` برای بازسازی خودکار تونل در صورت قطع اتصال.


* 
**مدیریت هوشمند:** ثبت به عنوان سرویس سیستم (`systemd`) برای اجرای خودکار پس از ریبوت.


* 
**امنیت:** استفاده از کلیدهای رمزنگاری شده SSH (بدون نیاز به پسورد).


* 
**ترافیک معکوس:** دور زدن فیلترینگ با استفاده از اتصال از خارج به ایران.



---

## 🛠 پیش‌نیازها

1. یک عدد سرور خارج (ترجیحاً اوبونتو 20.04 به بالا).
2. یک عدد سرور ایران (با پورت‌های آزاد).
3. دسترسی **Root** در هر دو سرور.

---

## 🚀 نصب و اجرا

برای شروع، تنها کافیست دستور زیر را در سرور خود اجرا کنید:

```bash
bash <(curl -Ls https://raw.githubusercontent.com//Reverse-SSH-Tunnel-Manager/main/run.sh)

```

> [!IMPORTANT]
> 
> 
> **مرحله اول:** ابتدا اسکریپت را روی **سرور ایران** اجرا کرده و گزینه `Setup IR Server` را انتخاب کنید تا تنظیمات `sshd_config` اعمال شود.
> 
> 
> 
> **مرحله دوم:** سپس اسکریپت را روی **سرور خارج** اجرا کرده و گزینه `Setup UK Server` را بزنید تا تونل برقرار شود.
> 
> 

---

## 🏗 ساختار فنی (Architecture)

در این سناریو:

1. سرور خارج (UK) یک اتصال SSH به سرور ایران (IR) برقرار می‌کند.


2. با استفاده از فلگ `-R` پورت‌های محلی سرور خارج روی آی‌پی ایران منتشر می‌شوند.


3. کاربران با اتصال به `IR_IP:PORT` در واقع ترافیک را به `UK_IP:PORT` می‌فرستند.



---

## 🔍 عیب‌یابی (Troubleshooting)

| مشکل | علت احتمالی | راه حل |
| --- | --- | --- |
| پورت‌ها روی ایران باز نمی‌شوند | عدم تنظیم GatewayPorts | بررسی فایل `/etc/ssh/sshd_config` و فعال بودن `GatewayPorts` 

 |
| تونل متصل نمی‌شود | بلاک بودن آی‌پی یا پورت ۲۲ | تست دستی با دستور `ssh root@IR_IP` 

 |
| سرویس غیرفعال است | خطای SSH Key | اجرای دستور `ssh-copy-id` به صورت دستی جهت اطمینان از صحت کلیدها 

 |

### مشاهده وضعیت و لاگ‌ها:

برای بررسی وضعیت تونل در سرور خارج از دستورات زیر استفاده کنید:

```bash
# مشاهده وضعیت سرویس
systemctl status reverse-tunnel

# مشاهده لاگ‌های زنده
journalctl -u reverse-tunnel -f
[cite_start]``` [cite: 14]

---

## 🤝 مشارکت
اگر پیشنهادی برای بهبود اسکریپت دارید، خوشحال می‌شویم **Pull Request** ارسال کنید یا در بخش **Issues** مطرح کنید.

---

**مایل هستید که بخش اسکریپت (فایل .sh) را هم بر اساس این استایل جدید (با رنگ‌بندی و منوهای زیباتر) آپدیت کنم؟**

```
